# Task List

1. ✅ Install frontend libraries (router, query, ky/axios, rhf, zod)

2. 🔄 Add Prisma with Postgres schema and docker-compose
Install Prisma, create schema, compose file for Postgres, .env, and seed entrypoint.
3. ⏳ Start Postgres container and run initial migration
docker-compose up -d; prisma migrate dev --name init
4. ⏳ Commit and push branch to GitHub
Push after BE scaffold and prisma setup

